{
  "name": "hugopallo",
  "version": "1.0.0",
  "description": "A Lambda function that downloads a source directory from S3, runs the Hugo static site generator on it, and uploads the result back to S3.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w"
  },
  "keywords": [
    "hugo",
    "lambda",
    "aws",
    "jalkapallo",
    "static",
    "site"
  ],
  "author": "David Dasenbrook",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "aws-sdk": "^2.382.0"
  },
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0"
  }
}
